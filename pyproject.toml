[project]
name = "masterclass-api"
version = "0.1.0"
description = "Environnement masterclass API"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jupyter>=1.1.1",
    "jupyter-cache>=1.0.1",
    "nbclient>=0.10.2",
    "nbformat>=5.10.4",
]
